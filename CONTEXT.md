# Laya System One

Laya 根据给定状态回答具有明确答案空间的问题，输出决策、评分或命题成立的概率。
本项目提供中文、英文和其他受模型支持语言的本地 System One 服务。

## Language

**System One**：
针对一个状态回答一组类型化问题的决策能力；答案来自预先定义的答案空间。
_Avoid_：文本生成、聊天补全。

**State（状态）**：
需要判断的文本或结构化信息，例如工单、邮件或业务记录。
_Avoid_：会话历史、系统提示词。

**Question（问题）**：
对状态提出的一项判断，由问题类型、指令和适用的评判标准描述。
_Avoid_：任意文本生成提示。

**Instructions（指令）**：
说明问题要判断什么的内容；与被判断的状态和候选项说明分别表达。

**Criteria（评判标准）**：
定义候选项、从低到高的评分等级，或真与假含义的标准。
其含义随问题类型变化。

**Choice（选择）**：
从有限候选项中选出一项，并给出各候选项的概率分布。
_Avoid_：多选、自由文本答案。

**Score（评分）**：
基于有序评判等级给出的期望等级，可为小数，并附带等级分布。
_Avoid_：仅取最高概率等级、默认百分制分数。

**Noul（命题概率）**：
某个命题成立的概率，取值范围为零到一。
_Avoid_：布尔答案、Choice 的通用置信度。

**Calibration（概率校准）**：
利用实际评估数据调整预测概率与观察频率之间的对应关系。
校准后的输出也不构成对单次结果的保证。
